import Link from 'next/link'; import { articles } from '@/lib/content'; import SiteHeader from '@/components/SiteHeader'; import SiteFooter from '@/components/SiteFooter';
export default function Category({params}:{params:{category:string}}){const name=decodeURIComponent(params.category); const shown=articles.filter(a=>a.category.toLowerCase()===name.toLowerCase()); return <><SiteHeader/><main className="mx-auto max-w-6xl px-4 py-10"><p className="text-sm font-semibold text-sky-700">KATEGORI</p><h1 className="mt-2 text-3xl font-bold capitalize">{name}</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{shown.length?shown.map(a=><article className="rounded-lg border bg-white p-5" key={a.slug}><h2 className="font-bold"><Link href={`/artikel/${a.slug}`}>{a.title}</Link></h2><p className="mt-2 text-sm text-slate-600">{a.excerpt}</p></article>):<p className="text-slate-600">Belum ada artikel pada kategori ini.</p>}</div></main><SiteFooter/></>}
