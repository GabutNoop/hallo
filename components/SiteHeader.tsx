import Link from 'next/link'; import { categories } from '@/lib/content';
export default function SiteHeader() { return <><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4"><Link href="/" className="text-xl font-bold text-sky-800">Hallo News</Link><nav className="hidden gap-4 md:flex">{categories.slice(0, 4).map(c => <Link key={c} href={`/berita/${c.toLowerCase()}`} className="text-sm text-slate-600 hover:text-sky-700">{c}</Link>)}</nav><Link href="/tentang-kami" className="text-sm text-slate-600 hover:text-sky-700">Tentang</Link></div></header></> }
